const state = {
    users: []
}

const getters = {
    allUsers: (state) => state.users
}

const actions = {
    async fetchUsers
}

const mutations = {}


export default {
  state,
  getters,
  actions,
  mutations,
};